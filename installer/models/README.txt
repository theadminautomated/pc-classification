# This directory should contain the manifests and blobs for your Ollama model.
# Example structure:
#   installer/models/manifests/<model-manifest-files>
#   installer/models/blobs/<model-blob-files>
#
# Copy the relevant files from your build machine's .ollama/models/ directory.
